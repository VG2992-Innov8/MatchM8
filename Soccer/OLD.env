# === Supabase ===
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# === Admin & Security ===
ADMIN_TOKEN=abc12345
PRED_TOKEN_SECRET=change-this-super-long-random-string
BCRYPT_ROUNDS=12
RATE_LIMIT_MAX_ATTEMPTS=5
RATE_LIMIT_WINDOW_MIN=15

# === App ===
PORT=3000
APP_BASE_URL=http://localhost:3000

# === Mail (for receipts & reminders) ===
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_pass
MAIL_FROM="MatchM8 <no-reply@matchm8.local>"
MAIL_DRY_RUN=true

# Optional (used by jobs/cron.js)
ACTIVE_WEEK=1

# Server
PORT=3000
COOKIE_SECRET=MatchM8-email-test-random-string
SEASON=2025
ACTIVE_WEEK=1

# Email (no real SMTP required in DRY_RUN)
MAIL_FROM="MatchM8 <no-reply@matchm8.local>"
MAIL_DRY_RUN=true

# --- App ---
PORT=3000
SEASON=2025
ACTIVE_WEEK=1
ADMIN_TOKEN=your_admin_token_here
CORS_ORIGIN=http://localhost:3000

# --- Email (receipts & reminders) ---
MAIL_FROM="MatchM8 <no-reply@matchm8.local>"
MAIL_DRY_RUN=true          # set to false for real SMTP
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_pass

# --- PIN / rate limits (already in your auth.js flow; keep or tweak) ---
PIN_MAX_ATTEMPTS=5
PIN_LOCK_MS=900000
