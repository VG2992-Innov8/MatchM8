# ---- Server (demo) ----
PORT=3001
CORS_ORIGIN=http://localhost:3001
APP_MODE=demo
DEMO_SKIP_LICENSE=true
DEMO_PLAYERS_MAX=5

# Admin auth for /api/admin/*
ADMIN_TOKEN=demoADMIN123!

# ---- Data (keep demo separate) ----
DATA_DIR=./data-demo

# ---- Email (optional) ----
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="MatchM8 <no-reply@matchm8.local>"

# ---- UI (optional) ----
APP_BASE_URL=http://localhost:3001
